function parrotTrouble(talking, hour) {
  return talking && (hour < 7 || hour > 20);
  
//   if (talking === true && (hour < 7 || hour > 20)) {
//     return true;
//   }
//   else {
//     return false;
//   }
}